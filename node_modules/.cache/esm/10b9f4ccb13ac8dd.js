_b13‍.x([["shuffle",()=>shuffle],["createPlayer",()=>createPlayer]]);let shuffle = (spielerNamen) => {
  for (let i = spielerNamen.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [spielerNamen[i], spielerNamen[j]] = [spielerNamen[j], spielerNamen[i]];
  }
  return spielerNamen;
};

       function createPlayer(names) {
  const players = {};

  names.forEach((name) => {
    players[name] = { name, punktzahl: 0 };
  });

  return players;
}
